import DeprecatedTransactionList from './DeprecatedTransactionList';

describe('<DeprecatedTransactionList />', () => {

    it('renders ok', () => {
        expect(<DeprecatedTransactionList />).to.be.ok;
    });

});
